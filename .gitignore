# Node / build artifacts (heredados de monet-app legacy)
node_modules/
.next/
out/
build/
.vercel/
.env*.local
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# References (repos externos pesados, se clonan on-demand)
references/

# OS
.DS_Store
Thumbs.db

# Local npm install artifacts in legacy app (not deployed via npm install -- Vercel rebuilds)
legacy/monet-app/package-lock.json
